## 💡 Key CSS

The magic happens with just two required properties:

| Element       | Property         | Purpose                                        |
| :------------ | :--------------- | :--------------------------------------------- |
| **`.parent`** | `display: flex;` | Establishes the Flexbox context.               |
| **`.child`**  | `margin: auto;`  | Distributes space equally, centering the item. |
